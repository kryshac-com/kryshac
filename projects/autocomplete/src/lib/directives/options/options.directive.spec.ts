import { KCOptionsDirective } from './options.directive';

describe('OptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new KCOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
