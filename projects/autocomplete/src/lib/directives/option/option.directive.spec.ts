import { KCOptionDirective } from './option.directive';

describe('OptionDirective', () => {
  it('should create an instance', () => {
    const directive = new KCOptionDirective();
    expect(directive).toBeTruthy();
  });
});
